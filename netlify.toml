[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # optional: set node version Netlify should use
  NODE_VERSION = "18"

[context.production.environment]
  NODE_ENV = "production"

[dev]
  command = "npm run dev"
  port = 5173
